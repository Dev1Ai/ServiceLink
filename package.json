{
  "name": "servicelink-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "db:generate": "pnpm --filter api exec prisma generate --schema=prisma/schema.prisma",
    "db:push": "pnpm --filter api exec prisma db push --schema=prisma/schema.prisma",
    "db:seed": "pnpm --filter api exec prisma db seed --schema=prisma/schema.prisma",
    "prisma:all": "pnpm --filter api prisma",
    "db:reset": "pnpm --filter api prisma migrate reset --force --skip-generate --skip-seed && pnpm db:seed",
    "db:studio": "pnpm --filter api prisma studio",
    "setup:e2e": "pnpm exec playwright install --with-deps",
    "setup:all": "pnpm install && pnpm --filter web build && pnpm run setup:e2e",
    "dev:e2e": "start-server-and-test \"pnpm --filter web dev -p 3000\" http://localhost:3000 \"pnpm exec playwright test\"",
    "test": "pnpm --filter api test",
    "dev:mobile-polished": "cd apps/mobile-uber-polished && expo start",
    "build:mobile-polished": "cd apps/mobile-uber-polished && expo build",
    "test:mobile-polished": "cd apps/mobile-uber-polished && pnpm exec jest"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "start-server-and-test": "^2.1.1",
    "turbo": "latest",
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
